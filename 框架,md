# 项目框架梳理：HelloAgents 智能旅行助手

基于 **LangGraph 多智能体框架** 构建的 AI 旅行规划应用，采用前后端分离架构。

## 一、整体架构

```
┌─────────────────────────────────────────────────────────┐
│                    前端 (Frontend)                       │
│            Vue 3 + TypeScript + Vite                     │
│            Ant Design Vue + 高德地图 JS API               │
│                    localhost:5173                         │
└──────────────────────┬──────────────────────────────────┘
                       │ HTTP / Axios
┌──────────────────────▼──────────────────────────────────┐
│                   后端 API 层 (FastAPI)                    │
│                  localhost:8000                           │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐               │
│  │ /api/trip│  │ /api/poi │  │ /api/map │               │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘               │
│       │              │              │                     │
├───────┴──────────────┴──────────────┴────────────────────┤
│                   服务层 (Services)                       │
│  ┌──────────────┐  ┌───────────────┐  ┌──────────────┐  │
│  │  llm_service  │  │ amap_service  │  │unsplash_svc  │  │
│  │  (LangChain)  │  │ (MCP Tool)    │  │ (图片搜索)    │  │
│  └──────┬───────┘  └───────┬───────┘  └──────────────┘  │
│         │                  │                              │
├─────────┴──────────────────┴─────────────────────────────┤
│                 Agent 层 (LangGraph 编排)                 │
│  ┌──────────────────────────────────────────────────┐   │
│  │     StateGraph: search(并行) → plan → fallback    │   │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐          │   │
│  │  │景点搜索  │ │天气查询  │ │酒店推荐  │ ┌──────┐  │   │
│  │  │ 节点     │ │ 节点     │ │ 节点     │ │规划  │  │   │
│  │  │ (Send)  │ │ (Send)   │ │ (Send)   │ │节点  │  │   │
│  │  └──────────┘ └──────────┘ └──────────┘ └──────┘  │   │
│  │  并行扇出(共享 MCP Tool 高德地图) → JSON mode 规划  │   │
│  └──────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
```

## 二、后端架构（backend/）

技术栈: Python + FastAPI + LangGraph + LangChain + MCP

| 文件 | 职责 |
|---|---|
| run.py | 启动入口，uvicorn 服务器 |
| app/config.py | 配置管理 (pydantic-settings)，读取 .env 环境变量 |
| app/api/main.py | FastAPI 主应用，注册 CORS 中间件和路由 |
| app/api/routes/trip.py | 核心路由 — POST /api/trip/plan 生成旅行计划 |
| app/api/routes/poi.py | POI 路由 — 搜索详情、Unsplash 景点图片 |
| app/api/routes/map.py | 地图路由 — POI 搜索、天气查询、路线规划 |
| app/models/schemas.py | Pydantic 数据模型（请求/响应） |
| app/services/llm_service.py | LangChain ChatOpenAI 单例（兼容 DeepSeek 等） |
| app/services/amap_service.py | 高德地图 MCP 服务封装 |
| app/services/unsplash_service.py | Unsplash 图片搜索服务 |
| app/agents/trip_planner_agent.py | **LangGraph 多智能体编排核心**（见下文） |

### 核心设计模式：LangGraph StateGraph 多智能体协作

改造后的编排不再使用 4 个独立的 SimpleAgent 串行调用，而是用 **LangGraph StateGraph** 将任务建模为有向图：

```
START
  │
  ├── Send(并行) ──► search 节点: 景点搜索 (maps_text_search)
  ├── Send(并行) ──► search 节点: 天气查询 (maps_weather)
  └── Send(并行) ──► search 节点: 酒店搜索 (maps_text_search)
  │
  └─────────────── 合并到 plan 节点 (JSON mode 结构化输出)
                        │
              ┌─────────┴─────────┐
              ▼                   ▼
          成功 → END          失败 → fallback → END
```

- **并行搜索**：三个搜索任务通过 `Send` 并行扇出，每个任务写入**独立状态字段**（`attractions_result` / `weather_result` / `hotels_result`），避免并发写同一字段互相覆盖。
- **状态流转**：`TripState` 类型化状态贯穿全图（请求 → 搜索结果 → 最终计划）。
- **结构化输出**：DeepSeek 等推理模型不支持 `response_format=json_schema`，因此规划节点使用 `response_format={"type": "json_object"}` + 正则提取 JSON 兜底，再经 Pydantic `TripPlan` 校验。
- **fallback 兜底**：搜索或规划任一环节失败时，经条件边路由到 fallback 节点生成备用计划，保证 API 稳定返回。
- **MCP 工具复用**：仍使用 `hello_agents.MCPTool`（`uvx amap-mcp-server`）调用高德地图，节点内通过 `asyncio.to_thread` 包装同步调用。

### 改造对比（旧 → 新）

| 维度 | 旧版 (HelloAgents SimpleAgent) | 新版 (LangGraph StateGraph) |
|---|---|---|
| 编排方式 | 4 个 SimpleAgent 串行 `agent.run()` | 图节点 + Send 并行扇出 |
| 搜索执行 | 串行（搜景点→查天气→找酒店） | **并行**，大幅缩短耗时 |
| 工具调用 | `[TOOL_CALL:...]` 手写语法 | 节点内直接调用 MCP 工具 |
| 结构化输出 | 正则提取 JSON + 手动解析 | JSON mode + 正则兜底 + Pydantic 校验 |
| 失败兜底 | 异常时 `_create_fallback_plan()` | 条件边路由到 fallback 节点 |
| LLM | HelloAgentsLLM | LangChain ChatOpenAI |

### 改造中修复的隐藏 Bug

1. **LLM 端点不匹配**：`.env` 中 `LLM_BASE_URL` 为 DeepSeek 的 Anthropic 端点（`/anthropic`），OpenAI 协议请求必 404。`llm_service.py` 自动归一化为 OpenAI 格式。
2. **推理模型 token 截断**：`deepseek-v4-pro` 等推理模型思考消耗大，`max_tokens` 不足会截断输出。已提升到 8192。
3. **并行写覆盖**：三个 Send 并行写同一字典会被覆盖，改为独立状态字段解决。
4. **uvx 不在 PATH**：Windows 下自动探测 venv 内 `uvx.exe`。

## 三、前端架构（frontend/）

技术栈: Vue 3 + TypeScript + Vite + Ant Design Vue + 高德地图 JS API

| 文件 | 职责 |
|---|---|
| index.html | 入口 HTML |
| src/main.ts | 应用入口 — 创建 Vue App、Vue Router、注册 Ant Design |
| src/App.vue | 根组件 — 顶部导航栏 + `<router-view>` |
| src/views/Home.vue | 首页 — 旅行表单（目的地、日期、偏好、额外要求） |
| src/views/Result.vue | 结果页 — 行程展示 + 高德地图 + 编辑/导出 |
| src/services/api.ts | Axios HTTP 客户端封装 |
| src/types/index.ts | TypeScript 类型定义 |
| vite.config.ts | Vite 配置 — @ 别名、API 代理到 :8000 |

页面路由：

- `/` → Home.vue（填写旅行信息）
- `/result` → Result.vue（展示旅行计划，含地图、编辑模式、导出图片/PDF）

数据流：

```
Home.vue → axios POST /api/trip/plan → sessionStorage → Result.vue
```

## 四、关键外部依赖

| 外部服务 | 用途 |
|---|---|
| LangGraph | 多智能体图编排（StateGraph + Send 并行） |
| LangChain (langchain-openai) | ChatOpenAI 封装，连接 DeepSeek 等 |
| hello_agents.MCPTool | 复用高德地图 MCP 工具连接 |
| 高德地图 MCP Server (amap-mcp-server) | POI 搜索、路线规划、天气查询（通过 MCP 协议） |
| LLM API (OpenAI/DeepSeek 等) | 驱动 Agent 推理和计划生成 |
| Unsplash API | 景点图片搜索 |
| 高德地图 JS API (前端) | 前端地图展示、标记、路线绘制 |

## 五、配置要点

需要配置的 API 密钥（在 backend/.env 和 frontend/.env 中）：

| 变量 | 说明 | 必需 |
|---|---|---|
| `AMAP_API_KEY` | 高德地图 **Web 服务** Key（后端） | ✅ |
| `LLM_API_KEY` | 大模型 API Key（OpenAI/DeepSeek 等） | ✅ |
| `LLM_BASE_URL` | LLM API 地址（兼容 OpenAI 格式，自动处理 Anthropic 后缀） | ✅ |
| `LLM_MODEL_ID` | 模型名称（如 deepseek-v4-pro） | ✅ |
| `VITE_AMAP_WEB_JS_KEY` | 高德地图 **Web 端 JS API** Key（前端） | ✅ |
| `UNSPLASH_ACCESS_KEY` | Unsplash API Key（可选） | 可选 |

> ⚠️ **重要**：后端 `AMAP_API_KEY` 必须是高德「**Web 服务**」平台的 Key。若误用 JS API 平台的 Key，调用会返回 `USERKEY_PLAT_NOMATCH` 错误。

## 六、启动方式

```bash
# 后端（Windows 需 UTF-8 编码避免中文乱码）
cd backend
PYTHONIOENCODING=utf-8 uvicorn app.api.main:app --host 0.0.0.0 --port 8000

# 前端
cd frontend
npm run dev   # 访问 http://localhost:5173
```

## 七、API 一览

- `POST /api/trip/plan` — 生成旅行计划（核心）
- `GET /api/trip/health` — 旅行规划服务健康检查
- `GET /api/map/poi` — 搜索 POI
- `GET /api/map/weather` — 查询天气
- `POST /api/map/route` — 规划路线
- `GET /api/poi/detail/{poi_id}` — POI 详情
- `GET /api/poi/photo` — 景点图片（Unsplash）
- `GET /health` — 服务健康检查
